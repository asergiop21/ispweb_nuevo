<%= simple_form_for([@customer, @ip]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ip_ap %>
    <%= f.input :ip_device %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
