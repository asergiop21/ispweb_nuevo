<!DOCTYPE html>
<html lang="es">
  <head>
    <meta name= "viewport" charset="utf-8" content="width=device-width, initial-scale=1.0" />
    <title>IspWeb 1.0</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield :head %>
  </head>
  <body>
        <%if user_signed_in?%>
          <%= render "layouts/menu" %>
          <div>
            <%= yield %>
          </div>
          <%= render "layouts/footer" %>
        <% else %>
          <div class="container">
            <%= yield %>
          </div>
        <% end %>
  </body>
</html>
