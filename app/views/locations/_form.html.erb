<%= simple_form_for @location, :defaults => {:input_html => {:class =>'span10'}} do |f| %>
  <div class="container">
    <div class="form-inputs">
      <div class="row-fluid">
        <%= f.error_notification %>
         <div class="span6">
              <%=f.input :name  %>
              <%=f.input :departament  %>
              <%=f.input :zipcode  %>
              <%=f.input :province  %>
              <%=f.country_select :country  %>
        </div>
      </div>
    </div>
  </div>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary'  %>
  </div>
<% end %>
