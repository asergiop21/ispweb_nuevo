<div>
  <div class="created_at"><%= ticket.created_at.strftime("%B %d, %Y") %></div>
  <div class="content">
<table class="table">
  <tr>
    <th>Issue</th>
    <th>Status</th>
    <th>Departament</th>
    <th>Priority</th>
    <th>User</th>
    <th>Path</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <tr>
    <td><%=link_to ticket.issue, customer_ticket_path(ticket.customer.id, ticket)%></td>
    <td><%= ticket.status %></td>
    <td><%= ticket.departament %></td>
    <td><%= ticket.priority %></td>
    <td><%= ticket.reply %></td>
    <td><%= ticket.user_id %></td>
    <td><%= ticket.path_id %></td>
    <td><%= ticket.customer_id %></td>
    <td><%= link_to "Reply", new_customer_ticket_path(:parent_id => ticket) %></td>
    <td><%= link_to 'Show', customer_ticket_path(ticket.customer.id, ticket) %></td>
    <td><%= link_to 'edit', edit_customer_ticket_path(ticket.customer.id, ticket) %></td>
    <td><%= link_to 'Destroy', customer_ticket_path(ticket.customer.id, ticket), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
</table>
<br />
  
  </div>

</div>
